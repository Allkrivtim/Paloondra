# Base URL (no trailing slash) of the backend from the previous step.
# REST calls go to `${VITE_API_BASE_URL}/api/...`, WebSockets to `ws(s)://<host>/ws/...`.
VITE_API_BASE_URL=http://localhost:4000
