#!/bin/sh
# Example RESTART_SCRIPT - copy to restart.sh. Just chains stop.sh + start.sh;
# adjust the paths if you rename anything.
set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

"$SCRIPT_DIR/stop.sh"
"$SCRIPT_DIR/start.sh"
